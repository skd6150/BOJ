dp

처음에는 링크드리스트+그리디로 비용이 낮은 순서대로 합치는걸로 풀었는데 안됨

psum[i]가 0~i까지의 부분합이고 dp[i][j]를 i~j까지 합하는 비용이라고 정의했을 때, i<k<j인 k에 대해서 dp[i][j] = max(dp[i][k] + dp[k][j]) + psum[j] - psum[i-1]임
i, j, k에 대해 반복하므로 O(N^3), 0~n까지 모두 합하는 비용은 dp[0][n-1]